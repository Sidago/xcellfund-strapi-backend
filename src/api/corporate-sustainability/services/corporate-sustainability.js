'use strict';

/**
 * corporate-sustainability service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::corporate-sustainability.corporate-sustainability');
