'use strict';

/**
 * vision-and-goal controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::vision-and-goal.vision-and-goal');
