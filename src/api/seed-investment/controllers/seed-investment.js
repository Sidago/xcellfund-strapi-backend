'use strict';

/**
 * seed-investment controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::seed-investment.seed-investment');
