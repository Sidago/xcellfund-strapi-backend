'use strict';

/**
 * convertible-debenture router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::convertible-debenture.convertible-debenture');
